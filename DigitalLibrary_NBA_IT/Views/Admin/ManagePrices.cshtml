@model IEnumerable<DigitalLibrary_NBA_IT.Models.Books>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manage Prices | Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Manage Book Prices</h1>

        @if (TempData["Message"] != null)
        {
            <div class="alert alert-info text-center">
                @TempData["Message"]
            </div>
        }

        <!-- סרגל חיפוש -->
        <div class="container mb-4">
            <form method="get" action="@Url.Action("ManagePrices", "Admin")">
                <div class="input-group">
                    <input type="text" name="query" class="form-control" placeholder="Search books by title or publisher..." value="@ViewBag.Query" />
                    <div class="input-group-append">
                        <button class="btn btn-primary" type="submit">Search</button>
                    </div>
                </div>
            </form>
        </div>

        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Publisher</th>
                    <th>Price</th>
                    <th>Update Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var book in Model)
                {
                    <tr>
                        <td>@book.Book_ID</td>
                        <td>@book.Title</td>
                        <td>@book.Publish</td>
                        <td>$@book.Price</td>
                        <td>
                            <form method="post" action="@Url.Action("UpdatePrice", "Admin")" class="form-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="bookId" value="@book.Book_ID" />
                                <div class="input-group">
                                    <input type="text" name="newPrice" class="form-control" placeholder="Enter new price" />
                                    <button type="submit" class="btn btn-primary">Update</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
